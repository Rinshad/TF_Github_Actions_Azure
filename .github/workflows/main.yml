name: 'Main Terraform Workflow'

on:
  push:
    branches:
      - master

jobs:
  call-terraform:
    uses: ./.github/workflows/terraform.yml
    with:
      auto_approve: false  # Passing input to the reusable workflow
    secrets: inherit       # Inherit secrets from the main workflow